[project]
name = "claude-code-port"
version = "0.1.0"
description = "Internal Python helpers extracted from Claude Code."
requires-python = ">=3.12"
dependencies = [
  "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = [
  "port/tests",
]
